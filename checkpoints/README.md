# Checkpoints

Pretrained model checkpoints are not included in this repository due to file size limitations.

Expected structure:

```text
checkpoints/
├── interval_transformer_best.pt
├── root_regressor_ann_deg25.pt
├── root_regressor_lstm_deg25.pt
└── root_regressor_mlp_deg25.pt
Place your trained checkpoints here before running full evaluation.
```